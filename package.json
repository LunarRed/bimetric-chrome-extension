{
  "name": "bimetric-build-tools",
  "version": "1.0.0",
  "description": "Build tools for Bimetric Chrome Extension",
  "main": "build.js",
  "scripts": {
    "build": "node build.js",
    "build:zip": "node build.js --zip-only",
    "build:crx": "node build.js --crx-only"
  },
  "devDependencies": {
    "archiver": "^5.3.1",
    "chalk": "^4.1.2"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
